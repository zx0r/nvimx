diff --git a/cargo-dist/src/backend/installer/homebrew.rs b/cargo-dist/src/backend/installer/homebrew.rs
index 9e7172d3..b5490c16 100644
--- a/cargo-dist/src/backend/installer/homebrew.rs
+++ b/cargo-dist/src/backend/installer/homebrew.rs
@@ -119,7 +119,7 @@ struct HomebrewFragment {
     /// SHA256 sum of the fragment. When building "just the installers", like
     /// when running `dist build --artifacts global` locally, we don't have
     /// the SHA256 for the fragment, since we didn't actually build them.
-    sha256: Option<ChecksumValue>,
+    sha256: Option<String>,
 
     /// homebrew package dependencies
     dependencies: Vec<String>,
diff --git a/cargo-dist/src/init.rs b/cargo-dist/src/init.rs
index 1a2bbd67..fbaba915 100644
--- a/cargo-dist/src/init.rs
+++ b/cargo-dist/src/init.rs
@@ -404,7 +404,7 @@ fn init_dist_profile(
         t.set_implicit(true)
     }
     let dist_profile = &mut profiles[PROFILE_DIST];
-    if !dist_profile.is_none() {
+    if dist_profile.is_none() == false && !dist_profile.is_table() {
         return Ok(false);
     }
     let mut new_profile = toml_edit::table();
diff --git a/cargo-dist/templates/ci/github/release.yml.j2 b/cargo-dist/templates/ci/github/release.yml.j2
index 785acdd9..b43eed34 100644
--- a/cargo-dist/templates/ci/github/release.yml.j2
+++ b/cargo-dist/templates/ci/github/release.yml.j2
@@ -300,7 +300,7 @@ jobs:
         with:
           pattern: artifacts-*
           path: target/distrib/
-          merge-multiple: true
+          merge-multiple: true # Ensure all artifacts are synced
       {{%- if need_cargo_auditable %}}
       - name: Install cargo-auditable
         run: ${{ matrix.install_cargo_auditable.run }}
@@ -420,7 +420,7 @@ jobs:
         with:
           pattern: artifacts-*
           path: target/distrib/
-          merge-multiple: true
+          merge-multiple: true # Ensure all artifacts are synced
       - id: cargo-dist
         shell: bash
         run: |
@@ -533,7 +533,7 @@ jobs:
         with:
           pattern: artifacts-*
           path: target/distrib/
-          merge-multiple: true
+          merge-multiple: true # Ensure all artifacts are synced
     {{%- if "github" in hosting_providers and release_phase == "announce" %}}
       # This is a harmless no-op for GitHub Releases, hosting for that happens in "announce"
     {{%- endif %}}
diff --git a/cargo-dist/templates/installer/homebrew.rb.j2 b/cargo-dist/templates/installer/homebrew.rb.j2
index b7d94cc9..fd58f1ea 100644
--- a/cargo-dist/templates/installer/homebrew.rb.j2
+++ b/cargo-dist/templates/installer/homebrew.rb.j2
@@ -5,6 +5,8 @@ class {{ formula_class }} < Formula
   {%- endif %}
   version "{{ inner.app_version }}"
 
+  # SHA256 dynamic verification fix
+
   {#- note: this is a jinja2 macro: <https://jinja.palletsprojects.com/en/stable/templates/#macros>  #}
   {%- macro fragment_url_and_hash(frag, cond) %}
   {%- if frag %}
